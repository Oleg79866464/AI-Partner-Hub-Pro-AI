# AI Catalog RU

Production-ready Russian AI tools directory for marketers and content creators.

## What is included
- Next.js 14 App Router homepage with server-side filtering
- Supabase PostgreSQL schema for tools, clicks, and analytics
- Python parser for `taaft.json` and a Supabase import helper
- Click tracking redirect endpoint
- Protected admin dashboard and CSV export
- Dynamic sitemap, robots.txt, metadata, and schema.org markup

## Environment variables
Copy `.env.example` to `.env.local` and fill in:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `ADMIN_SECRET_KEY`
- `NEXT_PUBLIC_SITE_URL`

## Deployment steps

### 1. Parser + seed workflow
```bash
cd parser
python3.11 -m venv venv
source venv/bin/activate
pip install pydantic==2.5.3 requests==2.31.0 beautifulsoup4==4.12.3 supabase==2.6.0
python cli.py --input taaft.json --output tools_clean.json --errors parsing_errors.json --sql-seed --apply-sql
```

What this does:
- parses `taaft.json`
- validates and cleans tools
- writes `tools_clean.json`
- generates `tools_clean.sql`
- applies the SQL seed directly to Supabase via `psql` when `SUPABASE_DB_URL` is set
- falls back to a readable SQL file you can run manually if needed

### 2. Supabase
1. Create a Supabase project.
2. Open the SQL editor.
3. Run `db/schema.sql`.
4. Set `SUPABASE_DB_URL` in `.env.local` if you want one-command SQL seeding.

If you prefer the Supabase SDK import path instead, use:
```bash
cd parser
python cli.py --input taaft.json --output tools_clean.json --errors parsing_errors.json --auto
```

### 3. Next.js
```bash
npm install
cp .env.example .env.local
npm run dev
```

For production deploy:

```bash
vercel --prod
```

## Affiliate program examples
- Jasper — PartnerStack, around 30% commission
- SurferSEO — Impact, around 25% commission
- Copy.ai — 20% commission
- Writesonic — partner program available
- Pictory — affiliate program available
- Descript — affiliate program available
- Frase — affiliate program available
- ChatGPT tools and integrations — use direct partner terms where available

## Monetization logic
We do not use CPC $0.05 clicks.

Revenue estimate formula:

```text
estimated_revenue = clicks × conversion_rate (0.15) × avg_product_price ($29) × commission_rate
```

Example:

```text
1000 clicks × 0.15 × $29 × 0.20 = $870/month
```

## Admin access
Open:

```text
/admin?key=YOUR_ADMIN_SECRET_KEY
```

## Changelog
- Premium B2B SaaS-style homepage with trust signals and strong CTA blocks
- Conversion-focused tool cards with affiliate-ready `/go/[id]` tracking
- Category SEO landing pages for high-frequency Russian queries
- Dynamic sitemap, robots.txt, and `ru-RU` metadata/snippets
- Schema markup: `ItemList`, `SoftwareApplication`, and `Organization`
- Supabase schema, click tracking, admin dashboard, and CSV export
- Python parser + CLI import/seed workflow with optional direct SQL apply
- Sample input and seed data for quick onboarding

## SEO notes
- Русские ключи: "нейросети для маркетинга", "ИИ для копирайтинга", "AI для SMM", "инструменты для SEO", "каталог нейросетей", "ИИ инструменты для маркетологов", "нейросети для контента", "AI для контент-маркетинга"
- OpenGraph locale: `ru_RU`
- schema.org `SoftwareApplication` and `ItemList` markup included
- Dynamic sitemap and robots.txt generated by Next.js
- Category SEO landing pages at `/categories/[slug]`
- Hreflang `ru-RU` configured via metadata alternates
